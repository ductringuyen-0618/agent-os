{
  "name": "@agentos/kernel",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist && node -e \"require('fs').copyFileSync('src/log/schema.sql','dist/schema.sql')\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentos/shared": "workspace:*",
    "zod": "^3",
    "better-sqlite3": "^12",
    "croner": "^9",
    "fastify": "^5",
    "@fastify/websocket": "^11",
    "@fastify/static": "^8",
    "yaml": "^2",
    "@modelcontextprotocol/sdk": "^1",
    "execa": "^9",
    "simple-git": "^3",
    "gray-matter": "^4",
    "nanoid": "^5",
    "pino": "^9"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7",
    "@types/node": "^22"
  }
}
