# SDD ledger — plan: docs/superpowers/plans/2026-09-08-agent-os-m4-techpulse-adapter.md

Spec: docs/superpowers/specs/2026-09-08-agent-os-design.md. Contract: docs/superpowers/plans/2026-09-08-agent-os-00-contract.md.
M1, M2, M3 are complete (see their own ledgers). M4 started same fire as M3's completion (M2/M3 finished this session, then continued straight into M4 with remaining budget).

## Task log

- Task 1 (frontmatter helpers) + Task 2 (`@agentos/adapters` package scaffold + registry): implemented together directly (Task 2 is required just to run Task 1's test — the package doesn't exist yet). Matches plan verbatim. **Flagged forward and fixed proactively**: the plan's Task 2 adds a `./adapters/types` export subpath to `packages/kernel/package.json` pointing at `dist/adapters/types.{js,d.ts}`, but kernel's build is a single-entry tsup bundle (`tsup src/index.ts ...`) that never produces that file — the exact same class of dist-vs-src gap already caught twice this fire (M1 Task 4's `schema.sql`, M2 Task 8's `syscall/bin.js`). Fixed proactively (not after a failure) by adding a third tsup invocation to kernel's build script: `tsup src/adapters/types.ts --format esm --dts --out-dir dist/adapters --clean=false`. Verified `dist/adapters/types.d.ts` (3.69 KB) exists post-build before running the adapters package's own tests against it. All 5 tests (adapters: 1+4) + full repo suite (153 tests) green. Merged.
- Task 3 (`sync()` — clone/fetch + mirror into `raw/`): implemented directly, matches plan verbatim (content-hash comparison for idempotency, `raw.added` emission only for genuinely new files). 7 adapters tests, full repo suite green. Merged.
- Task 4 (`sync()` — proposal.changed events + Decision creation): implemented directly, matches plan verbatim (title from first H1, body from "Why this increases engagement"/"Effort estimate" sections with a 1500-char fallback, dedupes on `{adapter, ref}`). 10 adapters tests (5 sync + 3 decisions + 2 from Task 3), full repo suite green. Merged.
- Task 5 (`applyDecision()` — flip, commit, push, record): implemented directly, matches plan verbatim (flip via `setStatus`, commit+push to `base_branch`, write `output/approvals/<date>-<slug>.md`, `wiki.writePage`, `resolveDecision`; on any failure resolves as `'error'` + `ops.alert` + re-throws). **Found and fixed a real test-isolation bug in the plan's own reference test**, not just an implementer mistake: `path.join(osRoot, '..', 'verify')` always resolves to the same fixed `os.tmpdir()`-relative path (`/tmp/verify`) regardless of `osRoot`'s unique `mkdtemp` suffix, so a leftover directory from a prior test run collides with `git clone` ("destination path already exists and is not an empty directory"). Reproduced empirically: passed when the `applyDecision.test.ts` file was run alone (first time `/tmp/verify` didn't exist), failed when the full adapters suite was re-run afterward (leftover dir from the standalone run). Fixed by giving the verify clone its own `mkdtempSync`'d directory; confirmed stable across 2 repeated full-suite runs after the fix. 12 adapters tests, full repo suite (161 tests: shared 12 + dashboard 8 + kernel 120 + cli 8 + adapters 12, + tools/fake-claude 4 via root aggregate) green. Merged.
- Pattern held again: 2 of 5 tasks so far (2, 5) surfaced a real latent bug in the plan's own reference code/build assumptions before it could bite a later task or a real (non-test) invocation — both were the same general class this project keeps re-encountering (a fixed/predictable path colliding across runs, or a build step the plan assumes exists but was never wired). Keep proactively checking for these rather than waiting for a task to fail.
- Task 6 (`AdapterHost.loadProjects`): implemented directly, matches plan verbatim (`${AGENTOS_HOME}`/`${AGENTOS_CLONES}` expansion, `ProjectConfigSchema.parse`). 2 tests, 122 kernel tests green. Merged.
- Task 7 (`AdapterHost.sync`/`applyDecision` — Run lifecycle): implemented directly, matches plan verbatim (contract addition `EventLog.getDecision`; `sync` creates/reuses a Run per project, `applyDecision` always creates its own). Deleted the now-obsolete M3 `adapterHost.stub.test.ts` (it pinned the exact fail-soft/always-throws placeholder behavior this task replaces — its own M3 docstring said "until M4 implements it"). **Found and fixed a real cross-milestone bug this surfaced**: `examples/os-template/os/routines.yaml`'s `techpulse-sync` routine (M3, already merged) set `adapter: techpulse-coo` — the `ProjectAdapter` *type* name — but `kernel.ts`'s `exec()` passes `routine.adapter` straight through as `AdapterHost.sync(projectName)`'s first argument, which matches by the project config's `name` field ("techpulse"), not its `adapter` field. Every fire of that routine would have failed with `unknown project 'techpulse-coo'` the moment M4 replaced M3's fail-soft stub with this real lookup. Fixed `routines.yaml` to pass the project name, with comments at both the yaml and the `kernel.ts` call site explaining the distinction (project name vs. adapter type) so it isn't lost again. 3 new tests, 127 kernel tests green. Merged.
- Task 8 (Decisions API + projects sync route): implemented directly, matches plan verbatim (`GET /api/decisions`, `POST /api/decisions/:id/{approve,reject}` with 404/409 handling, `POST /api/projects/:name/sync`). **Found and fixed a real gap in the plan's own test**: its `makeKernel()` helper never seeded a `projects/*.yaml`, so the "adapter-backed approve/reject" test's decision (adapter: techpulse-coo) hit Task 7's `AdapterHost.applyDecision`, which genuinely needs a matching project from `loadProjects()` to build the `AdapterContext` — with none seeded it always threw before reaching the registered adapter. Seeded a techpulse project config in the test helper (same shape Task 6/7's own tests already use). 4 tests, 127 kernel tests green. Merged.
- Task 9 (CLI `decisions`/`approve`/`reject`/`sync`): implemented directly. Fixed the plan's wrong subpath imports (`@agentos/kernel/log/eventLog` etc. → `@agentos/kernel`) and `ApiClient` constructor shape (object, not bare string) to match this repo's actual conventions, same class of deviation as M1/M3's ledgers already noted for the CLI. **Found and fixed a real, previously-latent production bug** while wiring the first CLI test that drives `ApiClient` through a REAL HTTP round trip (every prior CLI test mocked `ApiClient` entirely, so this was never exercised): `ApiClient.request()` unconditionally set `Content-Type: application/json` even for bodyless POSTs, and Fastify's default JSON body parser rejects an empty body under that header (`FST_ERR_CTP_EMPTY_JSON_BODY`) — this silently affected M3's `setRoutineEnabled` (enable/disable) too, just never caught. Fixed by only setting the header when `init.body` is actually present. 1 test, 9 cli tests green. Merged.
- Task 10 (TechPulse project config for os-template): content + one test, implemented directly. Fixed a real ESM bug in the plan's own reference test (bare `__dirname`, unavailable under this repo's ESM/`verbatimModuleSyntax` config) using the same `path.dirname(fileURLToPath(import.meta.url))` polyfill every other kernel test needing a filesystem-relative path already uses. 1 test, 128 kernel tests green. Merged.
- Task 11 (network-free e2e test): implemented directly, matches plan verbatim (proactively applied the Task 5 `verify`-dir mkdtemp fix rather than waiting to hit it again). **Passed on the first real attempt against `vitest run` — but `tsc --noEmit` caught a real type-level integration gap `vitest` couldn't see**: kernel's build bundled `src/adapters/types.ts` into `dist/adapters/types.{js,d.ts}` via its own separate tsup invocation (added in Task 2, to make the `"./adapters/types"` export subpath resolve at all) — tsup treats each entry as an independent bundle, so this produced a second, structurally-duplicated-but-nominally-different copy of every type transitively referenced by `adapters/types.ts` (down to `EventLog`'s private `db` field) from the copy already bundled into `dist/index.js`. Passing `adapterRegistry` (typed against one bundle's `AdapterContext`) into `AdapterHost` (typed against the other) failed to typecheck — caught only by `tsc --noEmit`, not by any test runner, since Vitest doesn't type-check. Fixed by replacing the separate tsup build with a two-line re-export shim (`dist/adapters/types.{js,d.ts}` = `export * from '../index.js'`), which is what that subpath always needed to be: the exact same declarations as the main entry, not a second copy. Verified clean `tsc --noEmit` across kernel/adapters/cli after the fix, plus a full clean-checkout simulation (`rm -rf packages/*/dist && pnpm install --frozen-lockfile && pnpm -r build && pnpm test`) — 170 tests + tools/fake-claude's 4 = 174, all green. Merged.
- **M4 COMPLETE.** All 11 tasks done. Pattern held for a fourth milestone straight: 6 of 11 tasks this milestone (5, 7, 8, 9, 10, 11) surfaced at least one genuine bug or gap, split roughly evenly between the plan's own reference code/tests and real cross-milestone integration seams (the routines.yaml/AdapterHost project-name mismatch, the tsup dual-bundle type-identity gap) — none were implementer mistakes. The type-identity gap is a new *class* of bug for this project's pattern log: a build/export-surface problem invisible to `vitest run` (no type checking) and only caught because `tsc --noEmit` was run as a matter of course before merging, not because any test failed. Worth remembering for M5/M6: **always run `tsc --noEmit` on every package that imports a cross-package subpath export, even when all tests are green** — this is exactly the kind of gap a green test suite alone would have shipped silently.
- STATE FOR CLOUD HANDOFF: M1 done, PR #1 (m1-kernel-core → main) open+green (title/body updated this fire to reflect M1+M2+M3), awaiting human merge — re-check its status at the start of the next fire, and consider updating its title/body again to add M4 now that it's done too. M2 complete. M3 complete. **M4 complete.** M5 (dashboard) has Tasks 1-4 done (pre-existing, reviewed by the repo owner before this cloud session started), Task 5+ remain — independent frontend work. M6 (release/hardening) has a docs batch already pulled forward (T6/T9/T11/T12, from M1's own fire) but its code tasks haven't started. Next fire's priorities in order: (1) re-check PR #1 status per the standing rule; (2) read `docs/superpowers/plans/2026-09-08-agent-os-m5-dashboard.md` Task 5 onward in full before starting — M5 Tasks 1-4 predate this session's own work and haven't been touched by any of the bug-hunting/ledger discipline established across M1-M4, so verify their actual state (build, test, lint) matches what the ledger claims before building on top; M5 is independent of M4/M6 and could run in parallel with M6 in a future fire with more budget.
