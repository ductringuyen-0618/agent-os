# Wiki Log

Append-only, one entry per `remember` call, newest at the bottom. Never
edit past entries.

## [2026-09-08] note | Template initialized
Seed entry for a freshly generated instance. Real entries are appended by
the `remember` syscall (`WikiService.writePage`) as agents run.
